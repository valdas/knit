bundle 
find dependancies
auto comment

BUG: in pre, i get the wrong substitutions

auto find dependancies

make the CRM download from tags

build markdown from eggs to ~/etc/doc


add Version=tags/2.1


q reader: sanitize inut file names (no meta characters)

DONE
====
awk now wak

markdown down ignore first para. also second para is  h1 and the rest are h2++

build egg files to ~/lib/var
get rid of Penny 
in dirs, make them optional set

VPATH working

rss reader. add a cache directory. 
	if tmp and last exists, return last.
	if not tmp and last less than 30 seconds, return last
	if not tmp and last is old
		wget to tmp
		return last

